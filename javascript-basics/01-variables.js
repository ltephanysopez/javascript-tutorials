// JavaScript variables are global by default
// When a JavaScript variable is initialized without declaration, it
// becomes a global variable

// To declare a variable in JavaScript, use the keyword var followed by
// the variable name

var number;
var name;
var myNumber;
var longValue = 111111;
var rate = 1.2;
var greeting = "Helo, world!";
var flag = false;
var myValue = 'a';
