// A JavaScript array is a JavaScript object. Array indices are property names that can be integers used to represent offsets

// One way to create an array is to declare an array variable using the [] operator

var numbers = [];

// When creating an array in the statement above, the array length is 0. You can get the length of an array by calling the built-in length property

print(numbers.length); // displays 0

// An array can also be declared with an array variable that has a set of elements inside the [] operator

// var numbers = [1, 2, 3, 4, 5];
// Array length = 5

// JavaScript array elements do not all have to be of the same type
var objects = [1, "JavaScript", true, null];
var nums = 3;
var = arr = [711, 411, 1776];

// We can verify an object is an array by calling the Array.isArray() function

console.log(Array.isArray(number)); // false
console.log(Array.isArray(arr));    // true 
